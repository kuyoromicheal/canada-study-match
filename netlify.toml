[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Next.js builds to ./dist (distDir). Netlify OpenNext adapter handles SSR/API routes
# automatically — do not set publish = "dist" for full-stack Next.js apps.
